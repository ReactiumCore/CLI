import {{name}} from './index';
import deps from 'dependencies';

export default {
    path: '{{route}}',
    exact: true,
    component: {{name}},
    load: (params, search) => deps.actions.{{name}}.mount(params, search),
};
